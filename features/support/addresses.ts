/**
 * The public address the specifications pretend the product is published at.
 *
 * It lives in support rather than in one of the step files, because two features assert against
 * it and step files do not import each other.
 */
export const WEB_URL = 'https://zaezd.example';
